# tinymce-plugin-adduser

## Getting Started

``` sh
$ npm install
$ cd /front npm install
$ cd /back npm install  
$ npm start
```

### MVP 

 + Плагин позволяет при нажатии @ вызывать список пользователей, осуществлять поиск по списку и вставлять имена пользователей в текстовый редактор.

  


